# Development-Docker
Repository to host the Dockerfile for generating the development Docker image. The development image is a pre-configured and customizable Docker image prepared for develop directly inside it.
