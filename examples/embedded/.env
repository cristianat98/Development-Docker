# Deliberately no DOCKER_HOST here: this example uses the daemon embedded in
# the dev container, and an explicitly set DOCKER_HOST makes the entrypoint
# defer to that external daemon instead (see ../README.md).
#
# Add your own credentials/tokens as needed — ../../base/.example.env
# documents every variable the entrypoint reads.
